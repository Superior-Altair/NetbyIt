import React from 'react';
import { Typography } from '@mui/material';

const TransactionList: React.FC = () => {
    return (
        <Typography variant="h4">
            Lista de Transacciones (En desarrollo)
        </Typography>
    );
};

export default TransactionList; 